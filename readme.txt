1. In terminal, run below command to install all dependent libraries.

pip install -r requirements.txt 


2. Open MySQL Workbench and connect to database


3. In termial, run below command to run the app.

python run.py


4. In your web browser, open http://localhost:9999/ to access index page.

5. Now you can play with the app. 

